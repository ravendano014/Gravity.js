function Particle(t,s,h,i,o,a,n){
    this.mass = t;
    this.x = s;
    this.y = h;
    this.vx = i;
    this.vy = o;
    this.ax = 0;
    this.ay = 0;
    this.type = a || "matter";
    this.name = n || "";
    this.color = [];
    
    // Propiedades del agujero negro
    this.isBlackHole = (this.type === "blackhole");
    if (this.isBlackHole) {
        this.color = [0, 0, 0]; // Negro absoluto
        this.radius = Math.sqrt(t) / 50; // Radio visual
        this.eventHorizonRadius = (2 * t) / 1000; // Radio de Schwarzschild simplificado (2GM/c^2)
        this.accretionDiskRadius = this.eventHorizonRadius * 3; // Disco de acreción
        this.name = "Black Hole (" + Math.round(t/1e4) + "M☉)";
    } else {
        this.type == "matter" 
            ? (this.color = [255, Math.round(256/(1+Math.pow(t/1e5,1))), Math.round(256/(1+Math.pow(t/1e4,1)))])
            : (this.color = [Math.round(256/(1+Math.pow(t/1e4,1))), Math.round(256/(1+Math.pow(t/1e5,1))), 255]);
        this.radius = Math.log(Math.E + t/1e3);
    }
    
    this.color[3] = (this.color[0] << 16 | this.color[1] << 8 | this.color[2]).toString(16);
    this.path = [];
    this.pathLength = 10000;
}function Particle(t,s,h,i,o,a,n){
    this.mass = t;
    this.x = s;
    this.y = h;
    this.vx = i;
    this.vy = o;
    this.ax = 0;
    this.ay = 0;
    this.type = a || "matter";
    this.name = n || "";
    this.color = [];
    
    // Propiedades del agujero negro
    this.isBlackHole = (this.type === "blackhole");
    if (this.isBlackHole) {
        this.color = [0, 0, 0]; // Negro absoluto
        this.radius = Math.sqrt(t) / 50; // Radio visual
        this.eventHorizonRadius = (2 * t) / 1000; // Radio de Schwarzschild simplificado (2GM/c^2)
        this.accretionDiskRadius = this.eventHorizonRadius * 3; // Disco de acreción
        this.name = "Black Hole (" + Math.round(t/1e4) + "M☉)";
    } else {
        this.type == "matter" 
            ? (this.color = [255, Math.round(256/(1+Math.pow(t/1e5,1))), Math.round(256/(1+Math.pow(t/1e4,1)))])
            : (this.color = [Math.round(256/(1+Math.pow(t/1e4,1))), Math.round(256/(1+Math.pow(t/1e5,1))), 255]);
        this.radius = Math.log(Math.E + t/1e3);
    }
    
    this.color[3] = (this.color[0] << 16 | this.color[1] << 8 | this.color[2]).toString(16);
    this.path = [];
    this.pathLength = 10000;
}
